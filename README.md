# lineapi-workshop-notify-nodejs

LINE Notify code sample for LINE API Workshop

# Setup
Generate LINE Notify token from https://notify-bot.line.me/my/
then update token in config.json

# Run
```
npm install
node index.js
```